unit FrHeaderApp;

interface

uses
  System.SysUtils, System.Types, System.UITypes, System.Classes, System.Variants, 
  FMX.Types, FMX.Graphics, FMX.Controls, FMX.Forms, FMX.Dialogs, FMX.StdCtrls,
  FMX.Effects, FMX.Objects, FMX.Layouts, FMX.Controls.Presentation, FMX.Edit;

type
  TheaderApp = class(TFrame)
    mostTop: TLayout;
    Rectangle1: TRectangle;
    headTop: TLayout;
    bg: TRectangle;
    logo_ss: TImage;
    container: TLayout;
    bg_recherche: TRectangle;
    shadowRech: TShadowEffect;
    loupe: TImage;
    layRecherche: TLayout;
    recherche: TEdit;
    Label1: TLabel;
    notif: TImage;
    admin: TImage;
  private
    { Déclarations privées }
  public
    { Déclarations publiques }
  end;

implementation

{$R *.fmx}

end.
